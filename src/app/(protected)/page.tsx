import SignInButton from '@/core/features/auth/components/SigninButton';

export default async function MainPage() {
  return (
    <div className="size-full flex-center">
      <SignInButton />
    </div>
  );
}
